# Красота с любовью — бьюти-пространство в Гатчине

Статический сайт, собранный по референсу клиента. Без сборки и зависимостей —
открывается как есть, кладётся на любой хостинг / GitHub Pages.

Живая версия: https://likes32.github.io/Krasota/

## Структура

```
index.html        Главная: хиро, «Наше пространство», «Направления»,
                  Обучение (#obuchenie), Мастер-классы (#masterclasses),
                  О нас (#about), CTA-полоса, подвал
services.html     Услуги: 6 направлений с записью, без цен
coworking.html    Коворкинг: преимущества, аренда (час/день/неделя/месяц),
                  типы рабочих мест, CTA-полоса
contacts.html     Контакты: блок записи (#zapis), реквизиты, статичная карта
styles.css        Вся вёрстка и токены (цвета/шрифты — в :root)
fonts.css         Playfair Display + Montserrat + Marck Script,
                  кириллица + латиница, вшиты base64 (внешних запросов нет)
main.js           Онлайн-запись, мобильное меню, тень шапки,
                  появление блоков при скролле
photos/           Фото-заглушки
photos/map/       12 тайлов OpenStreetMap для статичной карты
```

Пункты меню «Обучение», «Мастер-классы» и «О нас» — якоря на секции главной.
Коворкинг, Услуги и Контакты — отдельные страницы.

## Онлайн-запись — главное, что нужно подключить

Все кнопки записи на сайте помечены атрибутом `data-book`. Куда они ведут,
задаётся **одной строкой** в начале `main.js`:

```js
var BOOKING_URL = '';
```

Впишите сюда ссылку из DIKIDI / YCLIENTS (или другого сервиса записи) — и все
кнопки на всех страницах начнут открывать её в новой вкладке. Добавлять
атрибуты к новым кнопкам не нужно, просто ставьте им `data-book`.

Пока строка пустая, кнопки работают по запасным адресам: телефон и блок
записи на странице контактов (`contacts.html#zapis`).

## Что заменить перед сдачей

1. **Фото.** Всё в `photos/` — временные стоки с Pexels (свободная лицензия).
   Пропорции при замене: `hero.jpg` 6:5, галерея `space-1…4.jpg` 3:4,
   `edu/mk/about.jpg` 4:3, `coworking.jpg`, `contacts.jpg`, `services.jpg` 4:3.
2. **Цены аренды.** Из референса взяты только «от 250 ₽/час» и «от 1800 ₽/день».
   Неделя (7 500 ₽), месяц (22 000 ₽) и цены по типам рабочих мест
   (250–300 ₽/час) — придуманные заглушки, их нужно заменить.
3. **Состав рабочих мест** в секции «Какое место арендовать» описан по общей
   логике бьюти-коворкинга — сверить с тем, что есть в помещении на самом деле.
4. **Услуги.** Перечень направлений в `services.html` типовой, цены сознательно
   не указаны: у каждого мастера свой прайс. Список стоит сверить с клиентом.
5. **Контакты** в `contacts.html` и в подвалах — телефон, Instagram, адрес
   сейчас те, что были на референсе (телефон явно демонстрационный).
6. **Карта.** Тайлы в `photos/map/` отрисованы для точки 59.5648, 30.1285
   (Гатчина, ул. Красная). Если адрес другой — пересчитать номера тайлов
   для zoom 15 и перекачать их с `tile.openstreetmap.org`, затем поправить
   позицию `.map__pin` (left/top в процентах) в `styles.css`.
   Атрибуция OpenStreetMap обязательна — она в `.map__attr` и в подвале.
7. **Тексты секций** Обучение / Мастер-классы / О нас написаны нами: на
   референсе этих экранов не было, там только пункты меню.
8. **© 2024** в подвалах — обновить год при необходимости.

## Мелочи

- `?static` в адресе отключает анимации появления — удобно для скриншотов
  и снятия макетов.
- Палитра и шрифты меняются в одном месте — блок `:root` в начале `styles.css`.
- Шапка и подвал одинаковые на всех страницах: правите в одном файле —
  не забудьте перенести в остальные три.
- Внешних запросов у сайта нет вообще: шрифты, карта и фото локальные.
